import React from 'react';
import style from "../WindowChat.module.css";
export const WindowTextArea = () => {
    return (
        <textarea className={style.windowTextArea}>

        </textarea>
    );
};

export default WindowTextArea;